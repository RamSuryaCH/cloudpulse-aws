{
  "name": "cloudpulse-backend",
  "version": "1.0.0",
  "description": "Serverless AWS Lambda backend for CloudPulse AI",
  "main": "index.js",
  "scripts": {
    "build": "zip -r dist/lambda.zip index.js package.json"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0"
  }
}
